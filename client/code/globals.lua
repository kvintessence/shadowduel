return {
    camera = nil,
    world = nil,
    socket = nil,

    drawPhysics = false,
    drawFOV = false,

    layers = {
        localPlayer = 500,
        otherPlayer = 400,
        sound = 300,
        surroundings = 200,
        footprints = 100,
        background = 0,
    }
}
